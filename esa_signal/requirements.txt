# ESA Signal — Python dependencies
# Install with: pip install -r requirements.txt

# Telegram bot
python-telegram-bot==21.6

# Scheduler
APScheduler==3.10.4

# HTTP
requests==2.32.3

# Environment variables
python-dotenv==1.0.1

# AI  (0.100.0 installed — compatible with httpx 0.28+)
anthropic>=0.50.0

# Market data  (1.3.0 installed — uses curl-cffi, bypasses Yahoo rate limits)
yfinance>=1.0.0

# Timezone
pytz==2024.2
